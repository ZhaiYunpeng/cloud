spring:
#  application:
#    name: producer
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config_server
eureka:
  client:
    serviceUrl:
     defaultZone: http://localhost:8000/eureka/