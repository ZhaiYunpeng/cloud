spring:
#  application:
#    name: producer
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config_server
eureka:
  client:
    serviceUrl:
     defaultZone: http://zhaiyp.yutong.com:8000/eureka/